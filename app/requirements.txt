# Lumi — Mac app (the PWA host + ElevenLabs + fallback). Python 3.10+ recommended.
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
python-multipart>=0.0.9   # /api/turn accepts multipart (mic audio upload)
qrcode>=7.4          # /share QR page (pure-python SVG, no PIL needed)
# python-dotenv not needed — app.eleven_tts has a tiny stdlib .env loader.
